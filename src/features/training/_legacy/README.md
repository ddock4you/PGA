This folder contains legacy training quiz modules that are not used by the current Training flow.

Current implementation uses `src/features/training/contexts/QuizContext.tsx` + `src/features/training/hooks/useQuizGenerator.ts`.
